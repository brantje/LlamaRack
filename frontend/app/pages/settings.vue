<script setup lang="ts">
const manager = useManager()
const { apiBase, profile } = manager
</script>
<template><div><header class="page-header"><div><p class="eyebrow">RUNTIME</p><h1>Settings</h1><p class="muted">Detected backend and llama.cpp capabilities.</p></div><button class="ghost" @click="manager.refresh">Refresh</button></header><section class="panel"><p class="eyebrow">BACKEND</p><h2>Connection</h2><div class="settings-list"><div><span>Management API</span><code>{{ apiBase }}/api/v1</code></div><div><span>OpenAI endpoint</span><code>{{ apiBase }}/v1</code></div></div></section><section class="panel"><p class="eyebrow">LLAMA.CPP</p><h2>Binary capabilities</h2><div v-if="profile" class="settings-list"><div><span>Binary</span><code>{{ profile.path }}</code></div><div><span>Version</span><code>{{ profile.version || 'unknown' }}</code></div><div><span>Fingerprint</span><code>{{ profile.fingerprint.slice(0, 24) }}…</code></div><div><span>Discovered options</span><strong>{{ profile.options.length }}</strong></div></div><div v-else class="alert warning">llama-server could not be discovered. Management features still work, but model workers cannot start until the binary path is correct.</div></section></div></template>
